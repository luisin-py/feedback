[build]
  command = "echo 'No build needed'"
  publish = "."
  functions = "functions"

[plugins]
  [[plugins]]
    package = "@netlify/plugin-forms"
    [plugins.inputs]
      enable = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
